[project]
name = "week1"
version = "0.1.0"
description = "Build and lead week1 module"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "bs4>=0.0.2",
    "pydantic>=2.13.3",
    "ruff>=0.15.12",
]
